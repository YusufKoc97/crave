node_modules/
.expo/
dist/
web-build/
*.log
.DS_Store
.env
.env.local
expo-env.d.ts
.idea/
.vscode/
.claude/settings.local.json

# Expo prebuild output — regenerated by `expo run:ios` / `expo run:android`.
# Untracked on purpose: the local ios/ tree is ~900 MB of Pods + build
# artifacts, well past GitHub's file-size limit.
/ios/
/android/

# Supabase CLI scratch (linked-project ref, per-machine)
supabase/.temp/
